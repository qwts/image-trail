---
'image-trail': patch
---

Detachable sections are complete: the Queue section can now detach like Recent history and Settings, and the detach control supports drag-out — press and drag it to place the floating window exactly where you drop it (a plain click still detaches at the default spot).
