---
'image-trail': patch
---

Detached section windows now have standard window chrome: a minimize button collapses the window to its title bar (session-only), and a close (X) button restores the section back into the panel, replacing the text Restore button.
