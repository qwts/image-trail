---
'image-trail': minor
---

Detect single-image, gallery, and feed page contexts, expose a capability-aware React context switcher, and persist explicit per-host overrides in extension-owned settings.
