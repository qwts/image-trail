---
'image-trail': patch
---

Add the detachable-section pattern and pilot it on Recent history: a keyboard-accessible detach control moves the section into a floating extension-owned window (drag its title to move, Escape or Restore returns it), leaving a stable placeholder in the panel.
