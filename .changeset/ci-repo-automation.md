---
'image-trail': patch
---

Harden CI/CD and repo automation (#278): cancel superseded PR CI runs, run CI on pushes to main, grouped weekly Dependabot updates, a husky + lint-staged pre-commit hook, and a changesets release flow whose `changeset:version` script keeps `extension/manifest.json` in step with `package.json`.
