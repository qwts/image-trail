---
'image-trail': patch
---

Keep provider action pending through transient recovery probes and preserve request ordering so duplicate OAuth windows cannot open and stale status does not overwrite newer selection.
