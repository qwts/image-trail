# Codex execpolicy rules — GENERATED by scripts/bootstrap-codex-env.mjs.
# Regenerate after changing test-related npm scripts; do not hand-edit the
# generated sections. Loaded only when the project .codex/ layer is
# trusted; most-restrictive decision wins. Validate with:
#   codex execpolicy check --rules .codex/rules/process-guard.rules -- <cmd…>

# --- Forbidden: direct test runners that bypass the guard (invariant) ---

prefix_rule(
    pattern = ["node", "--test"],
    decision = "forbidden",
    justification = "Bypasses the process-tree memory guard; use the guarded npm test entrypoints.",
)

prefix_rule(
    pattern = ["npx", "playwright", "test"],
    decision = "forbidden",
    justification = "Bypasses the process-tree memory guard; use the guarded e2e entrypoint.",
)

prefix_rule(
    pattern = ["playwright", "test"],
    decision = "forbidden",
    justification = "Bypasses the process-tree memory guard; use the guarded e2e entrypoint.",
)

prefix_rule(
    pattern = ["npx", "c8"],
    decision = "forbidden",
    justification = "Bypasses the process-tree memory guard; use the guarded coverage entrypoint.",
)

prefix_rule(
    pattern = ["c8"],
    decision = "forbidden",
    justification = "Bypasses the process-tree memory guard; use the guarded coverage entrypoint.",
)

prefix_rule(
    pattern = ["npx", "test-storybook"],
    decision = "forbidden",
    justification = "Bypasses the process-tree memory guard; use the guarded stories entrypoint.",
)

prefix_rule(
    pattern = ["test-storybook"],
    decision = "forbidden",
    justification = "Bypasses the process-tree memory guard; use the guarded stories entrypoint.",
)

prefix_rule(
    pattern = ["npx", "vitest"],
    decision = "forbidden",
    justification = "Bypasses the process-tree memory guard; use the guarded npm test entrypoints.",
)

prefix_rule(
    pattern = ["vitest"],
    decision = "forbidden",
    justification = "Bypasses the process-tree memory guard; use the guarded npm test entrypoints.",
)

prefix_rule(
    pattern = ["npx", "jest"],
    decision = "forbidden",
    justification = "Bypasses the process-tree memory guard; use the guarded npm test entrypoints.",
)

prefix_rule(
    pattern = ["jest"],
    decision = "forbidden",
    justification = "Bypasses the process-tree memory guard; use the guarded npm test entrypoints.",
)

# --- Forbidden: unguarded inner scripts and human-only runs (derived) ---

prefix_rule(
    pattern = ["npm", "run", "test:unit:inner"],
    decision = "forbidden",
    justification = "Inner npm script skips the guard; use the guarded entrypoint.",
)

prefix_rule(
    pattern = ["npm", "run", "test:unit:run"],
    decision = "forbidden",
    justification = "Inner npm script skips the guard; use the guarded entrypoint.",
)

prefix_rule(
    pattern = ["npm", "run", "test:dom:inner"],
    decision = "forbidden",
    justification = "Inner npm script skips the guard; use the guarded entrypoint.",
)

prefix_rule(
    pattern = ["npm", "run", "test:dom:run"],
    decision = "forbidden",
    justification = "Inner npm script skips the guard; use the guarded entrypoint.",
)

prefix_rule(
    pattern = ["npm", "run", "test:run"],
    decision = "forbidden",
    justification = "Inner npm script skips the guard; use the guarded entrypoint.",
)

prefix_rule(
    pattern = ["npm", "run", "test:cov:inner"],
    decision = "forbidden",
    justification = "Inner npm script skips the guard; use the guarded entrypoint.",
)

prefix_rule(
    pattern = ["npm", "run", "test:stories:ci:inner"],
    decision = "forbidden",
    justification = "Inner npm script skips the guard; use the guarded entrypoint.",
)

prefix_rule(
    pattern = ["npm", "run", "test:e2e:inner"],
    decision = "forbidden",
    justification = "Inner npm script skips the guard; use the guarded entrypoint.",
)

prefix_rule(
    pattern = ["npm", "run", "test:e2e:ui:inner"],
    decision = "forbidden",
    justification = "Inner npm script skips the guard; use the guarded entrypoint.",
)

prefix_rule(
    pattern = ["npm", "run", "test:e2e:headed:inner"],
    decision = "forbidden",
    justification = "Inner npm script skips the guard; use the guarded entrypoint.",
)

prefix_rule(
    pattern = ["npm", "run", "test:inner"],
    decision = "forbidden",
    justification = "Inner npm script skips the guard; use the guarded entrypoint.",
)

prefix_rule(
    pattern = ["npm", "run", "test:e2e:ui"],
    decision = "forbidden",
    justification = "Interactive/headed run steals the user's desktop focus; human-only.",
)

prefix_rule(
    pattern = ["npm", "run", "test:e2e:headed"],
    decision = "forbidden",
    justification = "Interactive/headed run steals the user's desktop focus; human-only.",
)

# --- Allowed: guarded entrypoints and bounded gates (derived) ---

prefix_rule(pattern = ["npm", "test"], decision = "allow")
prefix_rule(pattern = ["npm", "run", "test:unit"], decision = "allow")
prefix_rule(pattern = ["npm", "run", "test:dom"], decision = "allow")
prefix_rule(pattern = ["npm", "run", "test:cov"], decision = "allow")
prefix_rule(pattern = ["npm", "run", "test:stories"], decision = "allow")
prefix_rule(pattern = ["npm", "run", "test:stories:ci"], decision = "allow")
prefix_rule(pattern = ["npm", "run", "test:e2e"], decision = "allow")
prefix_rule(pattern = ["npm", "run", "lint"], decision = "allow")
prefix_rule(pattern = ["npm", "run", "format:check"], decision = "allow")
prefix_rule(pattern = ["npm", "run", "typecheck"], decision = "allow")
prefix_rule(pattern = ["npm", "run", "build"], decision = "allow")
prefix_rule(pattern = ["npm", "run", "ci"], decision = "allow")
